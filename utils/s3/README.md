# Clean S3 Bucket

Clean a bucket from all of it files


**Instructions**

1. Install
```sh
pip install -r requirements.txt
```
2. Set env
```sh
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
```
3. Call the script
```sh
python clean_bucket.py --bucket my-bucket
```