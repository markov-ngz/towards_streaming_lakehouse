apiVersion: v1
kind: Service
metadata:
  name: flink-sql-gateway
  namespace: flink
spec:
  selector:
    app: flink-sql-gateway
  ports:
    - name: sql-gateway
      port: 8083
      targetPort: 8083